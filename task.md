# Воркшоп: Система изучения английского - Web + Telegram (Firebase Edition)

## Обзор проекта
Создаем единую экосистему для изучения английского, построенную на **Firebase**. Платформа включает веб-приложение для тестирования словарного запаса и Telegram-бота, который обогащает слова контекстом и использует интервальное повторение. Данные синхронизируются между приложениями в реальном времени.

**Формат:** 2 части - живое кодирование + домашнее задание.
**Основа:** ~3100 слов Oxford Dictionary (точное число **3144** из `words.json`), база данных **Firestore**.

---

## ЧАСТЬ 1: Web-приложение викторина

### Концепция
Создаем интерактивный сайт-викторину. Механика в стиле Tinder: свайп влево ("не знаю") или вправо ("знаю").

### Основные функции

#### 1. Аутентификация и хранение данных в Firestore
- **Анонимная аутентификация:** При первом входе для каждого пользователя автоматически создается анонимный аккаунт в **Firebase Authentication**. Это позволяет надежно хранить его прогресс в облаке.
- **Облачное хранилище:** Все данные (списки известных/неизвестных слов, прогресс, перемешанный плейлист) хранятся в **Firestore**. Это обеспечивает синхронизацию между разными устройствами и браузерами.
- **Автосохранение:** Каждое действие пользователя (свайп) в реальном времени сохраняется в Firestore.

#### 2. Интерфейс и статистика
- **Карточка со словом:** Показывает английское слово.
- **Управление:** Мышь, клавиатура, тач-жесты.
- **Прогресс-бар и счетчики:** Визуализация прогресса (пройдено X из 3144).
- **Случайный порядок и возобновление:** Приложение один раз перемешивает слова и сохраняет этот порядок в Firestore, что позволяет корректно возобновлять сессию с любого устройства.

#### 3. Синхронизация с Telegram-ботом
- **Привязка аккаунта:** В интерфейсе есть специальное поле для ввода кода. Пользователь получает этот код от Telegram-бота, чтобы связать свою веб-сессию с Telegram-аккаунтом.

#### 4. Управление данными и Экспорт
- **Сброс прогресса:** Кнопка для полного удаления данных пользователя из Firestore (с подтверждением).
- **Экспорт в PDF:** Возможность скачать PDF-отчет со списками известных/неизвестных слов и датой экспорта.

### Технические детали реализации
- Чистый HTML/CSS/TypeScript.
- **Firebase SDK** для веб (Auth и Firestore).
- jsPDF для генерации PDF.

---

## ЧАСТЬ 2: Telegram-бот с интервальным повторением

### Концепция
Telegram-бот, который в реальном времени получает данные из Firestore. Перед отправкой слова пользователю, бот **пытается обогатить его контекстом** (примером использования) через внешние API, делая обучение более эффективным.

### Основные функции

#### 1. Привязка аккаунта
- **Команда `/connect`:** В ответ на эту команду бот генерирует уникальный одноразовый код. Пользователь должен ввести этот код в веб-приложении для связывания аккаунтов.

#### 2. Логика отправки слова
- **Доступ к данным:** После привязки бот работает со списком неизвестных слов пользователя напрямую из Firestore.
- **Обогащение контекстом:** Перед отправкой бот делает запрос к бесплатному API словарей (например, Free Dictionary API) для поиска примера использования слова.
- **Адаптивный формат сообщения:**
  - **Если пример найден**, сообщение будет в богатом формате:
    ```
    I'd like some coffee. I need the **boost**.

    ||Я бы выпил кофе. Мне нужен **заряд бодрости**.|| - скрыто по умолчанию

    [😰 Сложно] [🤔 Нормально] [👍 Хорошо] [😎 Легко]
    ```
  - **Если пример не найден**, используется базовый формат:
    ```
    **boost**

    ||заряд бодрости|| - скрыто по умолчанию

    [😰 Сложно] [🤔 Нормально] [👍 Хорошо] [😎 Легко]
    ```

#### 3. Алгоритм интервального повторения
- **4 уровня сложности**, которые определяют дату следующего показа слова. Эта информация также хранится и обновляется в Firestore.

### Требования к реализации
- **Node.js + TypeScript**.
- **Firebase Admin SDK** для доступа к Firestore.
- Интеграция с внешним **API словарей** (например, Free Dictionary API).
- Библиотека `node-telegram-bot-api`.
- База данных: **Firestore** как единый источник данных.

---

## План развития и возможные улучшения

### 1. Группировка слов по темам
- **Этап 1 (Подготовка данных):** Написать отдельный скрипт, который проанализирует все 3144 слова с помощью AI (например, Gemini API) и добавит к каждому слову поле "тема" (`topic`).
- **Этап 2 (Реализация):**
  - **В веб-приложении:** Добавить фильтр для запуска викторины по конкретной теме.
  - **В Telegram-боте:** Добавить команду `/study_topic [тема]` для целевого изучения лексики.
  - **В статистике:** Отображать прогресс изучения в разрезе по темам.

### 2. Улучшение AI-контекста
- **Переход на генеративный AI:** В будущем можно заменить API словарей на более мощный генеративный AI (например, Gemini API) для создания нескольких уникальных и более подходящих по уровню сложности примеров для каждого слова.

---

## Ключевые навыки
- Создание интерактивных веб-интерфейсов.
- Работа с облачной базой данных **Firebase (Firestore)**.
- Реализация **анонимной аутентификации**.
- Разработка Telegram-ботов с интеграцией внешних API.
- Реализация алгоритмов интервального повторения в облаке.
- Построение синхронизированной экосистемы из нескольких приложений.
